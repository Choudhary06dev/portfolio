import { type CertificationType } from '@/types';

/**
 * Professional certification credentials.
 * Readonly array of certification variables.
 */
export const certifications: readonly CertificationType[] = [];
